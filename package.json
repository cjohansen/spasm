{
  "name": "spa-sm",
  "version": "0.16.0",
  "description": "Single Page Application State Manager - 'Just enough structure' for single page web apps",
  "main": "src/spasm.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "nodemon demo/index.js",
    "build": "browserify -s spasm -o spasm.js src/spasm.js",
    "postbuild": "uglify -s spasm.js -o spasm.js",
    "prepublish": "npm run build",
    "test": "mochify test/*.js test/**/*.js",
    "autotest": "mochify --watch test/*.js test/**/*.js"
  },
  "keywords": [
    "react",
    "spa",
    "data flow"
  ],
  "author": "Christian Johansen",
  "license": "ISC",
  "dependencies": {
    "react": "~0.13.3",
    "babel-runtime": "^5.8.25",
    "uglifyify": "^3.0.1"
  },
  "devDependencies": {
    "babelify": "~6.3.0",
    "browserify": "^11.1.0",
    "browserify-derequire": "^0.9.4",
    "browserify-middleware": "~7.0.0",
    "express": "~4.13.3",
    "formatio": "^1.1.2",
    "jasmine-core": "^2.3.4",
    "mochify": "^2.13.0",
    "nodemon": "~1.4.1",
    "referee": "^1.1.1",
    "referee-sinon": "^1.0.2",
    "sinon": "^1.17.0",
    "uglify": "~0.1.5"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "optional": "runtime"
        }
      ],
      "uglifyify"
    ],
    "plugin": [
      "browserify-derequire"
    ]
  }
}
